# .github/workflows/sonar.yml
name: Sonar Scan

on:
 workflow_call:

jobs:
  sonar:
    runs-on: java_build
    #needs: build  # Ensures Sonar runs after build succeeds
    steps:
      - name: SonarCloud Scan
        run: mvn sonar:sonar -Dsonar.projectKey=shivrajpatil7m_DevSecops_Pipeline -Dsonar.organization=shivrajpatil7m -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=ffd1cdb582332f3279d27fa9d64e74d9f33bcda6
